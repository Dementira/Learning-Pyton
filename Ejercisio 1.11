#Ejercicio 11
#Pide al usuario dos números y muestra la “distancia” entre ellos (el valor absoluto de su diferencia, de modo que el resultado sea 
siempre positivo).

def distancia(x,y):
    z= (x-y)
    if z < 0:
        z = z * (-1)
    return z

numero1 = int(input('Ingrese el primer operando: '))
numero2 = int(input('Ingrese el segundo operando: '))

print('La distancia o valor absoluto entre ', numero1, 'y', numero2, 'es: ', distancia(numero1, numero2))
