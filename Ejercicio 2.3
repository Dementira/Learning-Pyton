# Ejercicio 13
#Realizar un algoritmos que lea un número y que muestre su raíz cuadrada y su raíz cúbica. Python3 no tiene ninguna función predefinida 
#que permita calcular la raíz cúbica, ¿Cómo se puede calcular?

def raiz_cuadrada(x):
    return x**(1/2)

def raiz_cubica(x):
    return x**(1/3)

numero = int(input('Ingrese su número: '))

print(raiz_cuadrada(numero))
print(raiz_cubica(numero))
